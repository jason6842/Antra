// Render Tree
// Representing the structure of React components as a tree is called Render Tree.

// Module Dependency Tree
// An app's module dependencies is where we may export components, functions, or constants.
// 1. Each node in a module dependency tree is a module 
// 2. Each branch represents an import statement in that module.