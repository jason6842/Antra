// if you have a boolean data type, 
// the variable name should be an adjective.

const isSunny = true;

// conditional operator | ternary operator
const decision = isSunny ? "Go out" : "Stay in";




const person = {
    birthYear: 1990,
}

// if the value is falsy, the default value will be used
const birthYear = birthYear || 1875

// if the first value is truthy, the second value will be used
const value = true && 10;

